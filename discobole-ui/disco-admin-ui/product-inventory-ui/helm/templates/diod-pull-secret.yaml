{{- if .Values.registryCredentials.create -}}
kind: Secret
type: kubernetes.io/dockerconfigjson
apiVersion: v1
metadata:
  name: diod.productinventoryui-tech-pull-token-{{ .Values.environmentType }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "productinventoryui.labels" . | nindent 4 }}
data:
  .dockerconfigjson: {{ template "productinventoryui.regcredData" . }}
{{- end }}
