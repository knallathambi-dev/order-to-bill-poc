apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderorchestrationui.fullname" . }}
  labels:
    {{- include "orderorchestrationui.labels" . | nindent 4 }}
    name: {{ include "orderorchestrationui.fullname" . }}
    protocol: tcp
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "orderorchestrationui.selectorLabels" . | nindent 4 }}
