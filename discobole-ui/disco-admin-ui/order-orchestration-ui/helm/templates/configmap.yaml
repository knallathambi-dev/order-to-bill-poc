apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "orderorchestrationui.fullname" . }}-config
data:
  GATEWAY_URL: {{ .Values.orderorchestrationui.service.gatewayUrl | quote }}
  STANDALONE_MODE: {{ .Values.orderorchestrationui.service.standaloneMode | quote }}