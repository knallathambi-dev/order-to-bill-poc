{{- if .Values.registryCredentials.create -}}
kind: Secret
type: kubernetes.io/dockerconfigjson
apiVersion: v1
metadata:
  name: diod.orderorchestrationui-tech-pull-token-{{ .Values.environmentType }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "orderorchestrationui.labels" . | nindent 4 }}
data:
  .dockerconfigjson: {{ template "orderorchestrationui.regcredData" . }}
{{- end }}
