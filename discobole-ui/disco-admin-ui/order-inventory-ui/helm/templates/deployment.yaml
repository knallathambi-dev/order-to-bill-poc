# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "orderinventoryui.fullname" . }}
  labels:
    {{- include "orderinventoryui.labels" . | nindent 4 }}
    {{- include "orderinventoryui.shutdownLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "orderinventoryui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "orderinventoryui.selectorLabels" . | nindent 8 }}
        {{- include "orderinventoryui.monitoringLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "orderinventoryui.serviceAccountName" . }}
      containers:
        - name: order-inventory-ui
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "orderinventoryui.fullname" . }}-config
          env:
            - name: ENV_TYPE
              value: {{ .Values.environmentType }}
            - name: GATEWAY
              value: {{ .Values.orderinventoryui.service.gatewayUrl }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}