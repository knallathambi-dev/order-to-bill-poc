apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderinventoryui.fullname" . }}
  labels:
    {{- include "orderinventoryui.labels" . | nindent 4 }}
    name: {{ include "orderinventoryui.fullname" . }}
    protocol: tcp
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "orderinventoryui.selectorLabels" . | nindent 4 }}
