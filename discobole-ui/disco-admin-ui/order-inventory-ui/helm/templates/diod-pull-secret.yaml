{{- if .Values.registryCredentials.create -}}
kind: Secret
type: kubernetes.io/dockerconfigjson
apiVersion: v1
metadata:
  name: diod.orderinventoryui-tech-pull-token-{{ .Values.environmentType }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "orderinventoryui.labels" . | nindent 4 }}
data:
  .dockerconfigjson: {{ template "orderinventoryui.regcredData" . }}
{{- end }}
