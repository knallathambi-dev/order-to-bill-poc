{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orderinventoryui.serviceAccountName" . }}
  labels:
    {{- include "orderinventoryui.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}

---

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "orderinventoryui.serviceAccountName" . }}-namespace-reader
  labels:
    {{- include "orderinventoryui.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["configmaps", "pods", "services", "endpoints", "secrets"]
    verbs: ["get", "list", "watch"]

---

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "orderinventoryui.serviceAccountName" . }}-namespace-reader-binding
  labels:
    {{- include "orderinventoryui.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "orderinventoryui.serviceAccountName" . }}
  apiGroup: ""
roleRef:
  kind: Role
  name: {{ include "orderinventoryui.serviceAccountName" . }}-namespace-reader
  apiGroup: ""
{{- end }}