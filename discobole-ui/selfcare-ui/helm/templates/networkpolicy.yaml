apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "selfcareui.fullname" . }}-network-policy
  labels:
    {{- include "selfcareui.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "selfcareui.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  ingress:
  - {}
