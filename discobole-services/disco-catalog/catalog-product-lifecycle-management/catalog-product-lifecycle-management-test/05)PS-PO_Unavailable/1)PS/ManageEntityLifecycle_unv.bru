meta {
  name: ManageEntityLifecycle_unv
  type: http
  seq: 1
}

post {
  url: {{host}}/lifecycle/processManagement/v1/processFlow
  body: json
  auth: none
}

auth:basic {
  username: 
  password: 
}

auth:bearer {
  token: 
}

body:json {
  {
      "processFlowSpecification": "ManageEntityLifecycle",
      "@type": "ProcessFlow"
  }
}

vars:post-response {
  Next_task: res.body._links.nextTaskstoBePerformed[0].href
}

script:post-response {
  
  
}

tests {
  var jsonData = res.getBody();
  
  test("to verify Status code is 201", function () {
      expect(res.getStatus()).to.equal(201);
  });
}

docs {
  Request to start the lifecycle process 
}
