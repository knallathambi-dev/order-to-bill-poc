meta {
  name: DefineCategoryIdentityData
  type: http
  seq: 2
}

patch {
  url: {{Next_task}}
  body: json
  auth: none
}

auth:basic {
  username: 
  password: 
}

auth:bearer {
  token: 
}

body:json {
  {
    "characteristic": [
      {
        "name": "DefineCategoryIdentityData",
        "valueType": "Object",
        "value": {
          "categoryIdentityData": {
            "name": "test_category",
            "description": "test_category",
            "isRoot": true,
            "parentId": ""
          },
          "defineSubcategory": [],
          "associateProductOffering": []
        },
        "@baseType": null,
        "@schemaLocation": null,
        "@type": "ObjectCharacteristic"
      }
    ]
  }
}

vars:post-response {
  Next_task: res.body._links.nextTaskstoBePerformed[1].href
  category_id: res.body.characteristic[2].value
}

assert {
  res.status: eq 200
}

tests {
  var jsonData = res.getBody();
  
  
  
  test("to verify next task is validate", function () {
    expect(jsonData._links.nextTaskstoBePerformed[1].title).to.equals("CategoryCreation.validate");
  });
  
  
}
