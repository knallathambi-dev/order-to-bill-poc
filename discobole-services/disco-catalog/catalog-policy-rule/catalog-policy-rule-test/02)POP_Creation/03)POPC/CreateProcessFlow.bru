meta {
  name: CreateProcessFlow
  type: http
  seq: 1
}

post {
  url: {{host}}/pop/processManagement/v1/processFlow
  body: json
  auth: none
}

auth:basic {
  username: 
  password: 
}

auth:bearer {
  token: 
}

body:json {
  {
      "processFlowSpecification": "ProductOfferingPriceCreation"
  }
}

vars:post-response {
  Next_task: res.body._links.nextTaskstoBePerformed[0].href
}

assert {
  res.status: eq 201
}

tests {
  var jsonData = res.getBody();
  
  
  test("To verify that next task is consist ProductOfferingPrice Type", function () {
    expect(jsonData._links.nextTaskstoBePerformed[0].title).to.equals("ProductOfferingPriceCreation.selectPOPType");
  });
  
  test("To verify that cancel task is present", function () {
    expect(jsonData._links.nextTaskstoBePerformed[1].title).to.equals("ProductOfferingPriceCreation.productOfferingPriceCancel");
  });
  
}
